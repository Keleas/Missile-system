    %При инициализации по данным переданным с GUI в класса РЛС генерируется структура на выходе
    MFR = MFR_V2_GenerateMFRType(...
        Loc, n,...
        lambda, Dmax, Dmin, Tscan, Timp, V_res,...
        asim_res, elev_res, asim_res_trace_cf, elev_res_trace_cf,...
        elev1, elev2, Nasim, Nelev, ...
        Bandwidth, Noise, Gain, P_peak, Tr,...
        Ntraj,Nmissiles);
	

    %Запускается на каждой новой итерации расчета:
		1)Функция переводящая все траектории в нужный формат
		TG = MFR_V2_GenerateTrajectoryType(TG_ID, TG_R0X, TG_R0Y, TG_R0Z, ...
										   TG_VX, TG_VY, TG_VZ, TG_RCS);
		2)Функция симулирующая работу РЛС
		[MFR,MSG] = MFR_V2_Cycle(TG,MFR,dT);
		
	%Отсылать на ПБУ нужно содержимое MFR, благо в структуре по сути линейные массивы.